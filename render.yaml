services:
  - type: web
    name: mp3-backend
    env: node
    plan: free
    buildCommand: |
      npm install
      pip install -r requirements.txt
      apt-get update && apt-get install -y ffmpeg
    startCommand: node server.js